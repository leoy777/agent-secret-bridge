# Copy to /etc/agent-secret-bridge/asb.env with mode 0600 after reviewing the
# configuration. Generate the literal value with:
# asb config hash --config /etc/agent-secret-bridge/asb.yaml
ASB_CONFIG_SHA256=replace_with_64_hex_characters
